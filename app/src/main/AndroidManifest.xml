<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.diego.rotas">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icone_principal"
        android:label="Multipel Entregas"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAk1qNiwa1PA1zI9A4Rl9ma1OCiDeIaLGw" />

        <activity
            android:name=".mapa.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".telas.cadastrarUsuario" />
        <activity android:name=".telas.TelaPrincipalAdm" />
        <activity android:name=".telas.listarPedidos" />
        <activity android:name=".telas.TelaLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".telas.TelaPrincipalVendedor" />
        <activity android:name=".telas.TelaPrincipalMotorista" />
        <activity android:name=".telas.TelaPrincipalCliente" />
        <activity android:name=".telas.GerenciarCadastro" />
        <activity android:name=".telas.Configuracao" />
        <activity android:name=".telas.AlterarUsuario"></activity>
    </application>

</manifest>